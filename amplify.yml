version: 1
frontend:
  phases:
    build:
      commands:
        - echo "BollettaSmart - Static files ready"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
      - '!node_modules/**/*'
      - '!server/**/*'
      - '!shared/**/*'
      - '!src/**/*'
      - '!uploads/**/*'
      - '!package*.json'
      - '!tsconfig*.json'
      - '!vite.config.js'
      - '!tailwind.config.js'
      - '!postcss.config.js'